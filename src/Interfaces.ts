export interface userLoginType {
    username:string,
    password:string
}

export interface userRegType {
    name: string,
    email: string,
    password: string,
    cPassword: string,
  };

export interface keyValue {
    key: string,
    value: string
}